{
  "name": "Geckoboard",
  "description": "Build clear, effective data dashboards for your business that keep teams informed, motivated and aligned around your KPIs.",
  "auth": {
    "method": "Custom",
    "fields": [
      {
        "label": "API access key",
        "properties": {
            "parameter": "apiAccessKey",
            "fieldType": "text"
        },
        "readonly": false,
        "required": true,
        "visible": true
      }
    ]
  },
  "domain": "https://api.geckoboard.com/"
}
