{
    "name": "Receive an incoming webhook (json)",
    "description": "Receive an incoming webhook to start a new taskflow",
    "documentation": "",
    "fields": [
        {
            "label": "Payload",
            "properties": {
                "parameter": "payload",
                "fieldType": "text"
            },
            "readonly": false,
            "required": false,
            "visible": false
        },{
            "label": "Secret",
            "properties": {
                "parameter": "secret",
                "fieldType": "text"
            },
            "readonly": false,
            "required": true,
            "visible": true
        }
    ],
    "headers": {
        "Authorization": "{auth.field.secretKey}",
        "Content-Type": "application/json",
        "Accept": "application/json"
    },
    "method": "POST",
    "endpoint": "",
    "request": {
        "payload": "string_to_hash_or_null(field.payload)}",
        "secret": "{field.secret}"
    }
}
